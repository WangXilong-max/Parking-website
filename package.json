{
  "name": "parking-website-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "cd ParkItRight && npm install && npm run install:backend && npm run build",
    "start": "cd ParkItRight && NODE_ENV=production npm start",
    "dev": "cd ParkItRight && npm run dev:full"
  }
}


